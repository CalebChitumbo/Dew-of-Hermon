import 'package:flutter/material.dart';

/// Stub — implemented by the feature module port (see PORT_PLAN.md).
class TransportRequestDetailScreen extends StatefulWidget {
  final String id;
  const TransportRequestDetailScreen({super.key, required this.id});

  @override
  State<TransportRequestDetailScreen> createState() => _TransportRequestDetailScreenState();
}

class _TransportRequestDetailScreenState extends State<TransportRequestDetailScreen> {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('TransportRequestDetailScreen — coming soon'));
  }
}
