import 'package:flutter/material.dart';

/// Stub — implemented by the feature module port (see PORT_PLAN.md).
class ServiceChecklistScreen extends StatefulWidget {
  final String id;
  const ServiceChecklistScreen({super.key, required this.id});

  @override
  State<ServiceChecklistScreen> createState() => _ServiceChecklistScreenState();
}

class _ServiceChecklistScreenState extends State<ServiceChecklistScreen> {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('ServiceChecklistScreen — coming soon'));
  }
}
