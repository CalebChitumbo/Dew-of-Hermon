import 'package:flutter/material.dart';

/// Stub — implemented by the feature module port (see PORT_PLAN.md).
class BraaiDetailScreen extends StatefulWidget {
  final String id;
  const BraaiDetailScreen({super.key, required this.id});

  @override
  State<BraaiDetailScreen> createState() => _BraaiDetailScreenState();
}

class _BraaiDetailScreenState extends State<BraaiDetailScreen> {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('BraaiDetailScreen — coming soon'));
  }
}
