import 'package:flutter/material.dart';

/// Stub — implemented by the feature module port (see PORT_PLAN.md).
class PublicOrderScreen extends StatefulWidget {
  const PublicOrderScreen({super.key});

  @override
  State<PublicOrderScreen> createState() => _PublicOrderScreenState();
}

class _PublicOrderScreenState extends State<PublicOrderScreen> {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('PublicOrderScreen — coming soon'));
  }
}
