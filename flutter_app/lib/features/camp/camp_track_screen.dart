import 'package:flutter/material.dart';

/// Stub — implemented by the feature module port (see PORT_PLAN.md).
class CampTrackScreen extends StatefulWidget {
  const CampTrackScreen({super.key});

  @override
  State<CampTrackScreen> createState() => _CampTrackScreenState();
}

class _CampTrackScreenState extends State<CampTrackScreen> {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('CampTrackScreen — coming soon'));
  }
}
