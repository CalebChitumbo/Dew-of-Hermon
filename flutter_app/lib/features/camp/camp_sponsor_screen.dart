import 'package:flutter/material.dart';

/// Stub — implemented by the feature module port (see PORT_PLAN.md).
class CampSponsorScreen extends StatefulWidget {
  const CampSponsorScreen({super.key});

  @override
  State<CampSponsorScreen> createState() => _CampSponsorScreenState();
}

class _CampSponsorScreenState extends State<CampSponsorScreen> {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('CampSponsorScreen — coming soon'));
  }
}
