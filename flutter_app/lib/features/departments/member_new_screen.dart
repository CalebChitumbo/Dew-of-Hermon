import 'package:flutter/material.dart';

/// Stub — implemented by the feature module port (see PORT_PLAN.md).
class MemberNewScreen extends StatefulWidget {
  const MemberNewScreen({super.key});

  @override
  State<MemberNewScreen> createState() => _MemberNewScreenState();
}

class _MemberNewScreenState extends State<MemberNewScreen> {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('MemberNewScreen — coming soon'));
  }
}
