import 'package:flutter/material.dart';

/// Stub — implemented by the feature module port (see PORT_PLAN.md).
class EventReportsReviewScreen extends StatefulWidget {
  const EventReportsReviewScreen({super.key});

  @override
  State<EventReportsReviewScreen> createState() => _EventReportsReviewScreenState();
}

class _EventReportsReviewScreenState extends State<EventReportsReviewScreen> {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('EventReportsReviewScreen — coming soon'));
  }
}
