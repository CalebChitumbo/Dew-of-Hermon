import 'package:flutter/material.dart';

/// Stub — implemented by the feature module port (see PORT_PLAN.md).
class EventRolesScreen extends StatefulWidget {
  final String id;
  const EventRolesScreen({super.key, required this.id});

  @override
  State<EventRolesScreen> createState() => _EventRolesScreenState();
}

class _EventRolesScreenState extends State<EventRolesScreen> {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('EventRolesScreen — coming soon'));
  }
}
