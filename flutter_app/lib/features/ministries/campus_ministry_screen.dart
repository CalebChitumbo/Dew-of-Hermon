import 'package:flutter/material.dart';

/// Stub — implemented by the feature module port (see PORT_PLAN.md).
class CampusMinistryScreen extends StatefulWidget {
  const CampusMinistryScreen({super.key});

  @override
  State<CampusMinistryScreen> createState() => _CampusMinistryScreenState();
}

class _CampusMinistryScreenState extends State<CampusMinistryScreen> {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('CampusMinistryScreen — coming soon'));
  }
}
