import 'package:flutter/material.dart';

/// Stub — implemented by the feature module port (see PORT_PLAN.md).
class CampCheckInScreen extends StatefulWidget {
  const CampCheckInScreen({super.key});

  @override
  State<CampCheckInScreen> createState() => _CampCheckInScreenState();
}

class _CampCheckInScreenState extends State<CampCheckInScreen> {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('CampCheckInScreen — coming soon'));
  }
}
