import 'package:flutter/material.dart';

/// Stub — implemented by the feature module port (see PORT_PLAN.md).
class CampGateScreen extends StatefulWidget {
  const CampGateScreen({super.key});

  @override
  State<CampGateScreen> createState() => _CampGateScreenState();
}

class _CampGateScreenState extends State<CampGateScreen> {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('CampGateScreen — coming soon'));
  }
}
