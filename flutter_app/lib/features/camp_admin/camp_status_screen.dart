import 'package:flutter/material.dart';

/// Stub — implemented by the feature module port (see PORT_PLAN.md).
class CampStatusScreen extends StatefulWidget {
  const CampStatusScreen({super.key});

  @override
  State<CampStatusScreen> createState() => _CampStatusScreenState();
}

class _CampStatusScreenState extends State<CampStatusScreen> {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('CampStatusScreen — coming soon'));
  }
}
