import 'package:flutter/material.dart';

/// Stub — implemented by the feature module port (see PORT_PLAN.md).
class CampPassesScreen extends StatefulWidget {
  const CampPassesScreen({super.key});

  @override
  State<CampPassesScreen> createState() => _CampPassesScreenState();
}

class _CampPassesScreenState extends State<CampPassesScreen> {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('CampPassesScreen — coming soon'));
  }
}
