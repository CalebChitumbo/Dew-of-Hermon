import 'package:flutter/material.dart';

/// Stub — implemented by the feature module port (see PORT_PLAN.md).
class LatreouScreen extends StatefulWidget {
  const LatreouScreen({super.key});

  @override
  State<LatreouScreen> createState() => _LatreouScreenState();
}

class _LatreouScreenState extends State<LatreouScreen> {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('LatreouScreen — coming soon'));
  }
}
