<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Flutter tooling talks to the debug VM service over the local network. -->
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>
